# MoneyBook 開發歷程

逐次追加，只增不改。詳細的建構紀錄在 `docs/dev-log-*.html`，這裡是索引與摘要。

---

## 2026-09-22 基金淨值自動查詢的可行性調查

**需求**
使用者問「基金價格可以像股價在網路上查詢嗎」。股價已於 v1.16.0 做到自動更新
（FinMind，預設關閉），要評估基金淨值能不能比照辦理。

**做法**
逐一從瀏覽器實測 CORS（`curl` 不受 CORS 限制，測了不算數）。
對照組先確認測試環境有效：FinMind 股價端點回 200、16 筆資料。

**產出**
無程式碼變更。結論如下表。

| 來源 | 瀏覽器可直接抓 | 說明 |
|---|---|---|
| FinMind | 否 | 免 token 只有 TaiwanStockPrice / TaiwanStockInfo，無基金資料集 |
| 集保基金觀測站 | 否 | 無 Access-Control-Allow-Origin |
| 投信投顧公會 SITCA | 否 | 同上 |
| MoneyDJ | 否 | 同上 |
| Yahoo Finance | 否 | 同上 |
| 鉅亨 fund.api.cnyes.com | **不可靠** | 見下 |

**問題與修正**
鉅亨的 `/fund/api/v1/funds/{id}/nav` 一度從外部 origin 成功（A36004 安聯台灣科技，
淨值 803.93），看起來可行。深入驗證後推翻：

- 同一組 URL，A36004 穩定成功、A16003 穩定失敗
- 從 cnyes 自己的網域測，四檔全部 200 有資料 → 不是「該檔沒資料」
- 在成功的 URL 後面加一個**無關的查詢參數**（`&_=1`）就變成失敗

結論：那個 CORS 標頭是 CDN 快取的殘留，不是鉅亨真的開放跨網域。
建在上面會得到「時好時壞、看起來像自己程式有 bug」的失效模式 ——
比完全不能用更糟，違反專案原則第 2 條，不採用。

**附帶發現：匯率可以自動抓**
三個來源都通過 CORS，USD/TWD 數值一致：

| 來源 | USD/TWD | 日期 |
|---|---|---|
| exchangerate-api.com v4 | 31.75 | 2026-09-22 |
| open.er-api.com v6 | 31.7515 | 2026-09-22 |
| fawazahmed0 currency-api (jsDelivr) | 31.7907 | 2026-09-21 |

外幣基金目前唯一純手動的環節就是匯率，這一段可以自動化。

**待辦**
- 基金淨值維持手動輸入，直到出現穩定的來源
- 匯率自動更新：沿用股價那套同意機制（預設關閉），待使用者拍板
- 若日後要做淨值，唯一可靠路徑是自建代理（Cloudflare Worker 之類），
  但會多一個要維護的服務，且來源網站改版就會壞

---

## 2026-09-24 電腦端啟動器（.bat）

**需求**
使用者要一個 `.bat`，在電腦上直接啟動 MoneyBook。

**做法**
做兩支，分別對應兩種真正不同的用途：

| 檔案 | 開哪裡 | 用途 |
|---|---|---|
| `啟動 MoneyBook.bat` | GitHub Pages 正式版 | 平常記帳。與手機同一份資料來源 |
| `啟動 MoneyBook-本機版.bat` | `localhost:8811` | 測試改動、完全不連外網時 |

兩支都優先用 Chrome／Edge 的 `--app=` 模式開啟：沒有網址列與分頁，
看起來像一般桌面程式。找不到就退回預設瀏覽器。

**關鍵取捨：為什麼線上版是預設**
瀏覽器依「網址」隔離 IndexedDB，`localhost:8811` 與 `fctien.github.io`
是兩個不同的來源，**資料完全分開**。若預設開本機版，使用者會看到一本空白的帳本，
第一反應是「資料不見了」。因此主要的那支開線上版，本機版改名字並在註解與畫面上
講明白這件事。

**實作細節**
- `%~dp0` 取 .bat 所在資料夾再 `cd /d`，從任何地方（含桌面捷徑）啟動都正確
- UTF-8 無 BOM ＋ 開頭 `chcp 65001`。BOM 會被 cmd 當成第一道指令而出錯
- 行尾必須 CRLF
- 本機版先確認 `python` 存在，沒有就提示改用線上版，而不是丟一個 errorlevel
- 伺服器跑在前景（關視窗即停止），開瀏覽器的動作用 PowerShell 延遲 2 秒排到背景，
  避免伺服器還沒起來就開頁面

**驗證**
以替身指令（把實際啟動換成 echo）實跑，確認：

| 項目 | 結果 |
|---|---|
| 中文輸出 | 正常，無亂碼 |
| Chrome 偵測 | 找到 `C:\Program Files\Google\Chrome\Application\chrome.exe` |
| 組出的命令列 | `"chrome.exe" "--app=https://fctien.github.io/moneybook/"` |
| Python 偵測 | 通過，未落入錯誤分支 |
| `%~dp0` | 從專案外啟動仍解析到 `...\MoneyBook`，`tools\serve.py` 找得到 |

**待辦**
- 使用者若已用 v1.18.0 的「立即安裝」裝成 PWA，Windows 開始功能表就會有捷徑，
  那比 .bat 更好用；.bat 是給沒安裝或想開本機版的情況

---

## 2026-09-26 iPhone 版面修正（v1.18.1）

**需求**
使用者回報 iPhone 上「跑版」，附明細頁截圖：狀態列與「2026年9月」之間有一大段空白，
畫面左緣有一個灰色圓角條。

**做法**
以 393×852、430×932、390×844、375×667、360×800、320×568 六種尺寸重現，
並注入 `--safe-top` / `--safe-bottom` 模擬瀏海與底部橫條。
寫一支稽核腳本掃五個分頁的每一個元素，找「超出畫面寬度」與「內容被自己的容器截斷」。

**問題與修正**

1. **內層捲動容器的橡皮筋回彈**（推測就是截圖上那段空白）
   `html, body` 上已經有 `overscroll-behavior-y: none`，但真正在捲動的是 `.app__main`。
   iOS 捲到頂再往下拉時，這一層自己會橡皮筋，整個內容往下滑一截 ——
   截圖上那段空白與左緣的灰色圓角條（iOS standalone 的滑動返回指示器）
   一起出現，是手勢進行中被截下來的。已在 `.app__main` 補上 `overscroll-behavior-y: none`。

2. **金額被截斷**（可重現的真 bug）
   320px 螢幕上報表頁的 `+126,480` 被 ellipsis 切成 `+126,48…`。
   `.stat__value` 與 `.summary-card__value` 原本是固定字級。
   改用 `clamp()` 讓字級跟著螢幕寬度縮 —— 數字被切掉比字小一點嚴重得多。
   另外在 ≤360px 加一組 media query，把頁面內距、卡片內距與格線間距一起收窄。

**驗證**

| 尺寸 | 代表機種 | 五個分頁的溢出／截斷 |
|---|---|---|
| 320×568 | iPhone SE 一代 | 0 |
| 360×800 | 常見 Android | 0 |
| 375×667 | iPhone SE 2/3、8 | 0 |
| 390×844 | iPhone 12–14 | 0 |
| 393×852 | iPhone 14–16 Pro | 0 |
| 430×932 | Pro Max | 0 |

393×852 實際畫面：收入 1,130,797／支出 4,318／結餘 1,126,479 三個七位數都完整顯示，
月份列在 71px（= 59 safe-area + 12 padding），分頁標籤在 791–808px、完整落在畫面內。
291 項測試全數通過。

**未能重現的部分**
截圖上的灰色圓角條不是 App 的任何元素（查過全部 `position: fixed` 的規則），
研判是 iOS standalone 模式的滑動返回指示器。若修正後仍有異常，需要更多線索
（是哪一頁、固定發生還是滑動時才有）。

**待辦**
- 使用者手機上的版本需要更新才會生效：完全關掉 App 再開，等幾秒讓 Service Worker 換版

---

## 2026-09-26 底部留白過多（v1.18.2）

**需求**
使用者回報「底下留白又太多」，附記帳頁截圖：「完成」按鈕與分頁列之間空一大塊，
中間還多一條看不出是什麼的橫線。並補充上一次的版面問題是
「進入其他功能後回到主頁面時發生」。

**做法**
在 393×852 注入 safe-area 實測，逐一量 `.app` / `.app__main` / `.install-banner` /
`.tabbar` 的座標，並比對切換分頁前後有無飄移。

**問題與修正**

1. **`el.hidden = true` 沒有生效，元素還在佔空間**
   v1.18.0 加的安裝提示列平常是 `node.hidden = true`。但 `.install-banner`
   的 class 規則寫了 `display: flex`，而**作者樣式永遠贏過瀏覽器內建的
   `[hidden] { display: none }`**（跟優先權無關，是層疊順序）。
   結果：`hidden` 是 true、`getComputedStyle` 卻回 `flex`，元素保留
   上下內距與上框線共 21px —— 就是截圖上那條莫名其妙的橫線。
   修正：加一條全域的 `[hidden] { display: none !important; }`，
   把這一整類問題一次釘死，而不是只修這一個元素。

2. **`--safe-bottom` 被算了兩次**
   `.tabbar` 墊了 `padding-bottom: var(--safe-bottom)` —— 它在螢幕最底下，這是對的。
   但 `.view--ledger/assets/report/settings` 與 `.entry-bottom` 也各自墊了一次。
   這些內容在 `.app__main` 裡，而 `.app__main` 下面就是分頁列，
   根本碰不到 home indicator。兩邊都墊就是憑空多留一次，iPhone 上多出 34px。
   修正：只有真正貼著螢幕底部的元素才留 safe area ——
   `.tabbar`、`.sheet`、`.toast-host` 保留，捲動內容一律拿掉。

**效果**（393×852，safe-bottom 34px，記帳頁）

| | 修正前 | 修正後 |
|---|---|---|
| 「完成」按鈕底部 | 697 | 751 |
| 分頁列頂端 | 759 | 759 |
| 兩者間距 | 62px | **8px** |

多出來的 54px 還給內容，分類區因此多顯示一整排。

**驗證**
320／360／375／390／393／430 六種尺寸 × 五個分頁全掃，溢出與截斷皆為 0。
分頁列標籤在每個尺寸都完整落在畫面內。291 項測試通過。

**未能重現**
「進入其他功能後回到主頁面」的版面跑掉：實測 記帳→明細→記帳、
記帳→設定→記帳，`.app` / `.app__main` / `.view--entry` / 分頁列的座標
三次完全相同，捲動位置也都是 0，沒有飄移。
上一版（v1.18.1）補的 `.app__main` overscroll 應該就是那個症狀的成因
（截圖是下拉橡皮筋進行中拍到的）。若更新後仍會發生，需要知道
是進入哪一個功能、回到哪一頁。

**待辦**
- 請使用者更新到 v1.18.2 後回報是否還有跑版

---

## 2026-09-26 畫面沒有填滿螢幕（v1.18.3）

**需求**
使用者更新到 v1.18.2 後回報：iPhone 上「還是沒有全面展開，底下有空白」。

**問題**
`.app` 原本用 `height: 100dvh` 決定高度。`dvh` 在 iOS 上是會變動的 ——
工具列收合、鍵盤、手勢動畫都會改變它。只要算出來比螢幕短一點，
`.app` 下方就會露出 `body` 的背景色 `--c-bg: #f4f5f7`，
看起來就是「畫面沒展開，下面一塊淺色空白」。

前兩版修掉的是**內距重複**與**隱藏元素仍佔空間**，那兩個是真的 bug 也確實有效，
但它們解釋不了「App 整體比螢幕短」這件事 —— 根因在高度是「算」出來的。

**做法**
不再用 vh 單位算高度，直接把 App 釘在視口上：

```css
.app { position: fixed; inset: 0; display: flex; flex-direction: column; }
html, body { overflow: hidden; }
```

`position: fixed` + `inset: 0` 貼的是版面視口，不受工具列與鍵盤影響。
彈出面板與提示訊息都掛在 `document.body` 上、是 `.app` 的兄弟節點，
層級不受影響（實測 `.sheet-backdrop` 仍為 z-index 100，正常覆蓋）。

**同時新增：設定 → 關於 → 版面診斷**
這類問題在電腦上重現不了 —— 螢幕尺寸、safe area、是不是獨立 App、
工具列高度只有當下那台裝置知道。做一個畫面把這些數字列出來並可一鍵複製，
其中「畫面底部剩餘空白」不是 0 就代表 App 沒填滿。
使用者回報時不必再用文字描述畫面。

**驗證**

| 尺寸 | App 底部座標 | 視窗高 | 底部剩餘 |
|---|---|---|---|
| 320×568 | 568 | 568 | **0** |
| 393×852 | 852 | 852 | **0** |
| 430×932 | 932 | 932 | **0** |

五個分頁在三種尺寸下溢出與截斷皆為 0；`.app__main` 捲動正常
（scrollHeight 971 / clientHeight 759，設定 scrollTop 有效）。
291 項測試通過。

**問題與修正（過程）**
用 bash heredoc 寫 JS 時 `
` 又變成真的換行，造成字串字面值跨行的語法錯誤。
這在本專案已經是第三次。改用 Edit 工具修，並把該處改寫成
`[...].join(String.fromCharCode(10))`，避免再踩。

**待辦**
- 若 iPhone 上仍有空白，請使用者到「設定 → 關於 → 版面診斷」複製數字回報

---

## 2026-09-26 診斷畫面的複製按鈕找不到（v1.18.4）

**需求**
使用者回報「複製診斷資訊 沒看到」。

**問題**
按鈕本身有渲染（電腦上實測有），但被放在十幾列數字的**最後面**。
`.sheet__body` 雖然可以捲動，小螢幕上要捲一段才看得到 ——
使用者的合理反應就是「沒有這顆按鈕」。
把一個「出問題時才會用到」的按鈕藏在捲軸下面，等於沒做。

順帶發現 `.sheet { max-height: 88vh }` 也在用 vh。
backdrop 已經是 `fixed; inset: 0`、高度是確定的，改用百分比才不會
受 iOS 工具列伸縮影響而讓面板底部跑到畫面外。

**做法**
- 複製按鈕移到最上面，緊接在說明文字之後
- 加一塊 `.diag-text` 純文字區（`user-select: text`）：
  剪貼簿 API 在 iOS 某些情境會被擋掉，這塊可以長按選取當備援
- 按下去成功後把按鈕文字改成「✓ 已複製」，使用者才知道真的複製到了
- `.sheet` 的 `max-height` 由 `88vh` 改成 `88%`

**驗證**
320×568（最小的螢幕）開啟診斷畫面，按鈕座標 702、
`.sheet__body` 範圍 630–1068 → **不必捲動就完整可見**。
純文字備援區也正常渲染。291 項測試通過。

**待辦**
- 使用者需更新到 v1.18.4；若畫面仍未更新，需完全關閉 App 再開
  （Service Worker 會提示「已下載新版本，下次開啟時生效」）

---

## 2026-09-26 裝置回報的數字與後續診斷（v1.18.5）

**使用者回報的實測值（iPhone、iOS 18.7／Safari 26.5.2、DPR 3、已加到主畫面）**

```
視窗 innerHeight: 812      螢幕 screen.height: 874
safe-area 上: 62px         safe-area 下: 34px
App 高度: 812              App 底部座標: 812
畫面底部剩餘空白: 0
```

**讀出來的兩件事**

1. **App 本身沒問題**：高度 812＝innerHeight 812，底部剩餘 0。
   v1.18.3 把 `.app` 改成 `position: fixed; inset: 0` 之後，
   App 已經完整填滿網頁視口。

2. **視口本身比螢幕短 62px**：874 − 812 = 62，而且**剛好等於 safe-area 上緣**。
   這不是 CSS 能解決的 —— 是 iOS 給的視口就只有那麼大。

**還無法判定的關鍵**
缺的那 62px 在**上面**還是**下面**，兩種情況的修法完全相反：

| 情況 | 現象 | 修法 |
|---|---|---|
| 視口從螢幕 y=0 開始（有畫到狀態列底下） | 缺的在螢幕下方 | 視口高度問題，CSS 無解，要從 manifest／安裝方式下手 |
| 視口從 y=62 開始（iOS 把它往下推） | 缺的在上方 | `padding-top: var(--safe-top)` 是重複墊的，要拿掉 |

`index.html` 的 meta 全部正確（`apple-mobile-web-app-status-bar-style: black-translucent`、
`viewport-fit=cover`、manifest `display: standalone`），照規格應該是第一種，
但實測數字不符合，所以不能用推論下結論。

**做法**
診斷畫面補上判定用的欄位與工具：

- `innerWidth × innerHeight`、`screen 寬×高`、`availHeight`、`outerHeight`、
  `documentElement.clientHeight`
- **`視口在螢幕上的起點 screenY`** —— 決定性的一項，0 或 62 直接分辨上述兩種情況
- `視口比螢幕短` —— 一眼看出視口與螢幕的差距
- **「標示 App 邊界（截圖用）」**：給 `.app` 加紅色外框、分頁列頂端加藍線。
  數字說得出「短了 62px」，說不出「短在哪一邊」；截一張圖，紅框外面就是答案。

**驗證**
402×812 模擬環境下所有新欄位正常輸出，外框與藍線正確繪製（實測截圖確認）。
291 項測試通過。

**待辦**
- 等使用者回報 `screenY` 與標示邊界的截圖，再決定修法

---

## 2026-09-26 結論：視口比螢幕矮，不是 CSS 問題（v1.18.6）

**判定依據**
使用者傳來記帳頁截圖（v1.18.4，iPhone、螢幕 874、視口 812、safe-top 62）。
量測截圖上的座標：

| 量到的東西 | 螢幕座標（CSS 點） |
|---|---|
| 支出／收入／轉帳 分段列頂端 | 約 74 |
| 分頁列上緣 | 約 720 |
| 分頁列標籤 | 約 760–777 |
| App 內容結束 | 約 812 |
| 螢幕底部 | 874 |

分段列在 74 ＝ `padding-top` 62 ＋ 視圖內距 12，**表示視口從螢幕 y=0 開始、
內容確實畫到了狀態列底下**（`black-translucent` 有作用，safe-top 62 也用對了）。
因此少掉的 62 點在**下方**：視口只有 0–812，螢幕是 0–874。

**結論**
App 已經填滿它拿得到的全部空間（`App 底部剩餘空白: 0`）。
少掉的那塊 iOS 沒有給這個主畫面捷徑，**CSS 畫不到，不是這支程式能解決的**。

`812` 剛好是 iPhone X／XS／11 Pro 的視口高度，而裝置螢幕是 874。
研判是主畫面捷徑記住了舊機型的視窗大小 —— 換手機或從備份還原之後最常見。
`index.html` 的 meta 與 manifest 都正確，重新加到主畫面才會重新取用。

**做法**
不硬改 CSS 去遮掩（那會讓正常裝置變醜），而是把判斷寫進診斷畫面：
偵測到 `screen.height - innerHeight > 4` 時，直接顯示成因與做法，
並用警告樣式標明 **iOS 刪掉主畫面捷徑會一併刪掉資料，務必先匯出備份**。

**驗證**
模擬 402×812 視口、螢幕高度 874，診斷畫面正確顯示
「⚠ 視口比螢幕矮 62 點」與備份警告；`視口比螢幕短: 62`、`screenY: 0`。
291 項測試通過。

**待辦**
- 使用者依指示：匯出備份 → 刪除主畫面捷徑 → 重新加入 → 還原備份
- 若重裝後仍為 812，代表不是捷徑幾何快取的問題，要再找別的方向

---

## 2026-09-26 還原版面，改追「用久了才跑版」（v1.19.0）

**需求**
使用者補了關鍵一句：**「原來的版面是對的，只是在使用多次以後才會跑版」**，
並要求先把版面還原。

**這句話推翻了先前的方向**
前面三版（v1.18.1–1.18.3）都在改「靜態的 CSS 幾何」，
但靜態 CSS 不會「用久了才出問題」。方向從一開始就錯了 ——
我拿一張手勢進行中的截圖當成穩定狀態在量，愈修愈偏。

**還原**
`css/app.css` 的版面幾何全部退回 v1.18.0：

| 項目 | 退回成 |
|---|---|
| `.app` | `height: 100vh` / `100dvh`（取消 `position: fixed; inset: 0`） |
| `html, body` | 取消 `overflow: hidden` |
| `.app__main` | 取消 `overscroll-behavior-y: none` |
| `.view--*` 底部內距 | `calc(20px + var(--safe-bottom))` |
| `.entry-bottom` 底部內距 | `calc(8px + var(--safe-bottom))` |
| `.summary-card__value` | `font-size: 17px` |
| `.stat__value` | `font-size: 15px` |
| `.sheet` | `max-height: 88vh` |
| ≤360px 的 media query | 整塊移除 |

只保留兩處純新增、對既有畫面零影響的東西：
`[hidden] { display: none !important; }`（修 v1.18.0 提示列留下 21px 殘影的回歸）
與診斷用樣式。

**新的假設：iOS 鍵盤把視窗捲走之後沒捲回來**
鍵盤彈出時 iOS 會把**整個視窗**往上捲以露出輸入框，收起後不一定捲得回去。
`.app` 高度正好等於視窗，視窗一被捲走，整個 App 就往上位移、底下露出空白。
每打一次字（金額、備註、搜尋）都可能發生一次 —— 完全符合
「一開始好好的，用久了才跑版」。

**做法**
`keepWindowPinned()`：視窗永遠不該被捲動（所有捲動都在 `.app__main` 裡），
發現 `scrollY !== 0` 就拉回 0。但**不能見到捲動就拉** ——
鍵盤開著時那個位移是必要的，硬拉回去使用者會看不到正在打字的欄位。
因此只在這些時機校正：`focusout` 後 120ms 且焦點不在輸入框、
`visualViewport` 回到全高（鍵盤收起）、轉向、回到前景。

診斷畫面同時補上 `scrollY`、`body 高度`、`body 比視窗高` ——
跑版當下這三項能直接證實或推翻這個假設。

**驗證**
模擬視窗被捲走 150px，觸發 `focusout` 後回到 0。
還原後版面：App 0–812（＝innerHeight）、分頁列 719–812、標籤 751–768，
提示列 `display: none` 高度 0。291 項測試通過。

**待辦**
- 使用者更新後照平常用一陣子；若再跑版，先開診斷看 `scrollY` 是不是 0
- 若 `scrollY` 是 0 但仍跑版，代表不是鍵盤位移，要再換方向

---

## 2026-09-26 拆開「底部空白」的兩個成因（v1.19.1）

**需求**
使用者回報 v1.19.0（已還原版面）底下仍有空白、沒有滿版。

**把兩件事分開**
使用者的診斷數字顯示這是兩塊不同的東西疊在一起：

| 成因 | 大小 | 屬於誰 |
|---|---|---|
| iOS 只給這個主畫面捷徑 812 點的視口（螢幕 874） | 62 點 | **不在網頁裡，CSS 畫不到** |
| `--safe-bottom` 在 App 內被墊了兩次 | 34 點 | App 自己的 bug |

`App 底部剩餘空白: 0` 證明 App 已經填滿它拿得到的視口 ——
第一塊不管 `.app` 寫成 `100dvh`、`100%` 還是 `position: fixed` 都一樣，
只有重建主畫面捷徑（iOS 重新量螢幕）能解。

**本次改動**
使用者確認後，只把第二塊修回來：`.view--*` 與 `.entry-bottom` 不再墊
`--safe-bottom`（分頁列已經在螢幕最底下墊過了）。
其餘維持 v1.19.0 還原後的樣子。

**過程中的誠實記錄**
v1.19.0 依使用者要求「還原版面」時，我把這個 34 點的重複內距一併退了回去 ——
那是個真的 bug，與「原來的版面」無關（它從很早就存在，只是沒人注意）。
還原時沒有把「回歸」與「既有缺陷」分開，等於自己把修好的東西弄壞。
下次做 revert 要先分類再動手。

**驗證**（402×812、safe-bottom 34）
記帳頁「完成」按鈕底部 711、分頁列頂端 719 → 間距 8 點（原本 42 點）。
App 0–812＝innerHeight。291 項測試通過。

**待辦**
- 使用者要自行重裝主畫面捷徑（已備份）；完成後看診斷的「視口比螢幕短」是否為 0

---

## 2026-09-26 重裝無效，改用可切換的實驗開關（v1.19.2）

**需求**
使用者依指示備份、刪除、重新加到主畫面、還原資料，
「視口比螢幕短」**仍然是 62** —— 主畫面捷徑幾何快取的假設不成立。

**目前確定的事實**
- iPhone、iOS 18.7／Safari 26.5.2、DPR 3、已加到主畫面
- `screen.height` 874、`innerHeight` 812、`safe-area 上` 62、`safe-area 下` 34
- `App 底部剩餘空白: 0` —— App 已經填滿它拿得到的視口
- 874 − 812 = 62，**剛好等於上緣 safe-area**
- `index.html` 的 meta 與 manifest 都正確，重裝後不變

**剩下的兩種可能，修法相反**

| 視口的位置 | 缺口在哪 | 該怎麼修 |
|---|---|---|
| 從螢幕 y=0 開始（內容畫到狀態列底下） | 下方 | 視口大小問題，CSS 無解 |
| 從 y=62 開始（iOS 已排除狀態列） | 上方 | `padding-top: var(--safe-top)` 是第二次墊，要歸零 |

`window.screenY` 在 iOS standalone 不保證可靠，從截圖量測也已經被推翻過一次
（第一張是手勢進行中拍的）。**不再猜。**

**做法**
做成使用者一按就能當場分辨的開關，而不是再賭一次：

- `store.setSafeTopOverride()` / `applySafeTopOverride()`：把 `--safe-top` 強制歸零，
  存進設定（`safeTopOverride`），啟動時套用
- 診斷畫面新增「**試：取消上方內距**」按鈕，可來回切換
- 判讀方式寫進提示文字：版面因此對了 → 是第二種（我們墊了兩次）；
  內容跑到瀏海底下 → 是第一種

這個開關不是暫時的除錯碼 —— 不論結果是哪一種，它都會留著當作
「裝置回報值對不起來時」的逃生口。

**驗證**
切換後 `.app__main` 的 `padding-top` 由 62px 變 0px，按鈕文字同步改成「↩ 復原上方內距」，
重新載入後仍然記住。291 項測試通過。

**待辦**
- 使用者按下開關後回報：版面對了，還是內容被瀏海擋住
- 若是前者，把偵測自動化（或至少把開關講清楚放在設定裡）

---

## 2026-09-26 更新送不到手上；視窗高度記錄器（v1.20.0）

**使用者回報兩件事**
1. **9:21 那個版本的畫面是對的、是滿版的**
2. **看不到 v1.19.2**

第 2 點比較嚴重 —— 更新送不到，前面做的都是白做。

**根因：Service Worker 從來沒有被要求檢查新版**
`sw.js` 有 `skipWaiting()` 與 `clients.claim()`，但 `registerServiceWorker()`
**從來沒有呼叫 `reg.update()`**。瀏覽器只在「導航」時才自動檢查新版的 SW，
而獨立 App 可能被留在背景好幾天都不會導航一次 ——
使用者就這樣卡在舊版本，而且完全沒有徵兆。

修正：
- 啟動時與**每次回到前景**都 `reg.update()`
- 新 SW 接手（`controllerchange`）後標記 `updateReady`；
  下次回到前景、且沒有開著面板也沒在輸入時，自動 `location.reload()`
  換上新程式碼（不能說換就換，使用者可能正在打字）
- 設定 → 關於新增「**🔄 檢查更新**」手動入口，直接告訴使用者是最新版還是有新版

**第 1 點改變了問題的形狀**
「剛更新完是滿版、用一陣子才跑版」表示 **`innerHeight` 會在使用途中變小而且不再回來**。
這只發生在真實裝置上，電腦重現不了，而且事後問「現在多少」也來不及 ——
要知道的是「什麼時候從多少變成多少」。

因此加了視窗高度記錄器：在啟動、`resize`、轉向、回到前景時記下 `innerHeight`，
保留**啟動以來的最大值、最小值與最後一次**，各自帶時間與觸發原因，
顯示在診斷畫面。若使用者跑版後回報「最大 874、最小 812（10:07・回到前景）」，
就一次同時證實了症狀、幅度與觸發時機。

資料只存在本機設定裡，不送出任何地方。

**驗證**
「檢查更新」回報「已經是最新版本 v1.20.0」；
記錄器在視窗由 812 變 700 時正確記下 `min: 700（resize）`、`max: 812`。
（模擬器改視窗大小不會發出原生 resize 事件，需手動派送才測得到，這是測試環境的限制。）
291 項測試通過。

**待辦**
- 使用者需要**手動跨過這一次**：舊版程式沒有自我更新的能力。
  完全關掉 App 再開兩次，或更新後用「檢查更新」。之後就自動了。
- 拿到跑版後的「視窗高｜最大／最小」再判斷 62 點是怎麼消失的

---

## 2026-09-26 更新機制的第二道問題：HTTP 快取（v1.20.1）

**需求**
使用者仍未更新到 v1.20.0。

**先排除「不是我的錯」的可能**
直接抓線上檔案確認，排除部署沒生效這條：

| 檢查 | 結果 |
|---|---|
| `https://fctien.github.io/moneybook/js/app.js` | `APP_VERSION = '1.20.0'` ✓ |
| `https://fctien.github.io/moneybook/sw.js` | `CACHE_VERSION = 'moneybook-v1.20.0'` ✓ |
| `sw.js` 的 `Cache-Control` | `max-age=600` |

網站沒問題。問題在裝置上。

**第二道問題**
v1.20.0 修的是「從來不呼叫 `reg.update()`」。但就算呼叫了，
瀏覽器檢查 `sw.js` 時**預設會走 HTTP 快取** ——
GitHub Pages 給的是 `max-age=600`，十分鐘內重開好幾次，
每次拿到的都是快取裡的舊 `sw.js`，於是「重開就會更新」這個直覺會失效。

修正：`navigator.serviceWorker.register('./sw.js', { updateViaCache: 'none' })`，
檢查 SW 腳本時一律略過 HTTP 快取。

**這一版救不了現在卡住的使用者**
修更新機制的程式碼本身，要靠更新機制才送得到 —— 先有雞還是先有蛋。
使用者必須手動跨過這一次，之後才會自動。

**驗證**
`registration.updateViaCache === 'none'`，版本 v1.20.1。291 項測試通過。

**待辦**
- 給使用者兩條路：等十分鐘以上再完全關閉重開兩次；或再重裝一次捷徑（已有備份）
- 跨過去之後，後續更新應可自動完成（啟動與回到前景都會檢查，且不走 HTTP 快取）

---

## 2026-09-26 底板同色；下緣 safe-area 覆寫（v1.21.0）

**使用者回報（已更新到 v1.20.1）**
1. 「**恢復上方內距是正確的**」—— 試過開關後確認上方內距要留著
2. 「**下方的版面有露底板**」

**第 1 點終於把分支定下來**
上方內距是需要的 → **視口從螢幕 y=0 開始**、內容有畫到狀態列底下
→ 少掉的 62 點確實在**下方**。
先前用截圖量測得到同樣結論，但那個方法已經被推翻過一次（第一張是手勢進行中拍的），
現在由使用者實際操作驗證，可以定案了。

**第 2 點：那塊畫不到，但可以讓它不顯眼**
視口只有 812、螢幕 874，差的 62 點在網頁外面，CSS 畫不到它 ——
但系統是拿**文件的畫布底色**去填那一塊的。
畫布底色預設來自 `body`（`--c-bg` 淺灰），而分頁列是白的（`--c-surface`），
於是分頁列下面出現一條顏色不同的帶子，看起來像破了個洞。

```css
html { background: var(--c-surface); }
```

把畫布底色改成跟分頁列同色，那一塊就變成分頁列的自然延伸。
`body` 的底色不動，App 內部外觀完全不變（實測 `body` 仍是 `--c-bg`）。

**這是遮掩不是修好**，註解裡寫明了 —— 那 62 點本來就拿不回來。

**順帶：下緣 safe-area 也是錯的**
視口既然沒延伸到螢幕底部，home indicator 就不在視口裡，
分頁列還照著 `env(safe-area-inset-bottom)` 墊 34 點就是白白吃掉空間。
比照上緣做成可切換的開關（`safeBottomOverride`），
`applySafeTopOverride` 改名為 `applySafeAreaOverrides` 並保留舊名稱別名。

**驗證**
`html` 的背景色與分頁列完全一致（深色模式下同為 `rgb(30,41,59)`），
`body` 維持 `--c-bg`。
下緣開關：注入 34px 後分頁列內距為 34px，開啟覆寫後變 0px，關閉後復原。
291 項測試通過。

**待辦**
- 使用者回報底板是否還看得出來、以及取消下方內距後分頁列是否更合理
- 兩個開關若都確定該開，考慮依 `screen.height - innerHeight` 自動判斷

---

## 2026-09-26 「檢查更新」自己被快取攔截（v1.21.3）

**需求**
使用者已在 v1.20.1，按「檢查更新」卻抓不到 v1.21.0。

**先排除部署**
線上 `sw.js` 是 `moneybook-v1.21.0`、`app.js` 是 `1.21.0`、CSS 也含新規則。網站沒問題。

**兩個疊在一起的 bug**

1. **拿 `reg.waiting` 當判斷依據**
   `sw.js` 呼叫了 `skipWaiting()`，新的 worker 會**直接跳過 waiting** 進入啟用，
   所以 `reg.waiting` 幾乎永遠是 `null`。
   `updateReady` 又要等 `controllerchange` 才會是 true，而那在 `reg.update()`
   回來之後才觸發。兩個都看不到 → 一律回報「已經是最新版」。

2. **改成比對版本號之後，探測請求自己被 Service Worker 攔截**
   改用 `fetch('./js/app.js')` 讀線上版本 —— 但 SW 的 fetch 處理是 cache-first，
   `caches.match()` 比對完整網址，於是讀到的是**快取裡的舊檔**，
   比對結果永遠相等。`fetch` 的 `cache: 'no-store'` 管的是 HTTP 快取，**擋不住 SW**。

   實測記錄：瀏覽器跑 1.21.1、磁碟已是 1.21.2，探測仍回報 `live: 1.21.1`。

**做法**
- 探測網址加上每次都不同的參數：`./js/app.js?v=${Date.now()}`。
  `caches.match()` 比對完整網址，帶參數就命不中快取、必定走網路。
  這招對**舊版的 SW 也有效**，不需要對方先更新。
- `sw.js` 的 fetch 處理明確放行帶 `?v=` 的請求，不要把探測網址存進快取
- 「檢查更新」判到有新版時呼叫 `forceUpdate()`：清掉程式快取再重載
  （只清 Cache Storage，**不動 IndexedDB 的記帳資料**）

**驗證（端到端）**

| 步驟 | 結果 |
|---|---|
| 瀏覽器 1.21.2、磁碟 1.21.3、且有 `moneybook-v1.21.2` 快取 | `{ live: '1.21.3', state: 'ready' }` |
| 按「檢查更新」 | 顯示「有新版本 v1.21.3，正在更新…」 |
| 自動重載後 | 版本 1.21.3、快取換成 `moneybook-v1.21.3` |
| 版本相同時 | `state: 'latest'`，顯示「已經是最新版本」 |

291 項測試通過。

**待辦**
- 使用者仍在 v1.20.1，那一版的「檢查更新」按鈕是壞的，按了沒用。
  但 v1.20.1 的**自動路徑**（啟動與回到前景時 `reg.update()`、
  `controllerchange` 後於下次回到前景重載）是好的 ——
  請改用「切出去再切回來」或「完全關閉再開兩次」，不要按按鈕。

---

## 2026-09-26 底部完全還原到 9:21 之前（v1.22.0）

**需求**
使用者已更新到 v1.21.3，底板仍有問題，指示：**先恢復到 9:21AM 之前的底板位置**。

**9:21 之前是哪一版**
查 commit 時間，09-26 當天第一次改動是 09:29（v1.18.1）。
9:21 時線上跑的是 **v1.18.0**（09-13 部署）。

**還原的三項**

| 項目 | 改回 |
|---|---|
| `html { background: var(--c-surface) }`（v1.21.0 加的畫布底色） | 整條移除 |
| `.view--*` 底部內距（v1.19.1 拿掉了 safe-bottom） | `calc(20px + var(--safe-bottom))` |
| `.entry-bottom` 底部內距（同上） | `calc(8px + var(--safe-bottom))` |

比對確認：`css/app.css` 現在與 v1.18.0 的差異只剩兩處純新增 ——
`[hidden] { display: none !important; }`（修 v1.18.0 提示列殘影的回歸）
與診斷用樣式，兩者都不影響既有版面。

**驗證**（402×812、safe-top 62、safe-bottom 34）

| 量測 | 結果 | v1.18.0 應有 |
|---|---|---|
| `html` 背景 | `rgba(0,0,0,0)` | 無（已移除） |
| `.entry-bottom` 下內距 | 42px | 8 + 34 ✓ |
| `.tabbar` 下內距 | 34px | 34 ✓ |
| `.app` | 0–812 | 填滿視口 ✓ |

291 項測試通過。

**必須說清楚的事**
這次還原的是 **App 內部的間距**。螢幕 874、視口 812 的那 62 點差距
**不是還原能解決的** —— 那塊在網頁外面。
v1.21.0 的畫布同色是想讓它不顯眼，使用者認為不對，已移除。

**待辦**
- 請使用者確認診斷裡「上方／下方內距已手動歸零」兩項都是「否」
  （那兩個開關是後來加的，9:21 當時不存在，開著就不是還原後的狀態）
- 底部仍有問題的話，需要知道現在看到的與 9:21 當時的差別具體在哪

---

## 2026-09-26 版面位置完全回到 v1.18.0（v1.22.1）

**需求**
使用者：「維持新版 OK，但將版面改回 9:21AM 之前的 UI 位置」。
也就是功能與修正都保留，但**幾何位置**要與 v1.18.0 一致。

**找出最後的差異**
v1.22.0 已把 CSS 的版面規則全部還原，把 v1.18.0 到 HEAD 的差異全部列出來後，
只剩一條會動到位置的：

```css
[hidden] { display: none !important; }
```

那是 v1.18.2 加的。它修的是：`.install-banner` 的 class 寫了 `display: flex`，
會蓋過瀏覽器內建的 `[hidden]{display:none}`（作者樣式永遠贏過 UA 樣式），
於是 `node.hidden = true` 之後元素還在，留下
`padding 10 + 10 + border 1 = 21px` 的空帶與一條上框線，夾在內容與分頁列之間。

在已安裝成獨立 App 的裝置上，提示列一定是隱藏的 ——
所以 9:21 當時，那條 21px 的空帶**一直都在**。它就是最後的位置差異。

**做法**
移除該規則，位置回到 v1.18.0。
註解裡寫清楚這條規則原本修的是什麼、為什麼被拿掉，免得日後有人再「修」一次。

確認影響範圍：全專案只有 `.install-banner` 的 class 設了 `display`；
另一處用到 `hidden` 的是 `funds.js` 的匯率欄位（`.field` / `.hint`），
那兩個 class 沒有設 `display`，瀏覽器內建規則本來就管得到，不受影響。

**驗證**（402×812、safe-top 62、safe-bottom 34、提示列已隱藏）

| 量測 | 結果 |
|---|---|
| `.install-banner` | `hidden=true`、`display: flex`、高 21px（698–719） |
| `.app__main` | 0–698 |
| `.tabbar` | 719–812 |
| `.entry-bottom` 下內距 | 42px |

與 v1.18.0 的 CSS 差異至此只剩診斷用樣式（`.app.is-outlined`、`.diag-text`），
兩者都只作用在平常不存在的元素上。**版面幾何與 v1.18.0 完全一致。**
291 項測試通過。

**立場記錄**
那條 21px 空帶我仍然認為是缺陷（一個隱藏元素不該佔空間）。
但使用者兩次明確要求恢復 9:21 之前的位置，而它正是唯一的差異 ——
位置是使用者的判斷，缺陷與否是我的判斷，這次照使用者的。

---

## 2026-09-26 數字鍵盤與分頁列各往下 20px（v1.23.0）

**需求**
使用者直接下指示：**把數字鍵盤與底部面板往下移 20 像素**。

**做法**
`.entry-bottom` 與 `.tabbar` 都是**底部錨定**的（在 flex column 的最後，
`flex-shrink: 0`，下緣貼著容器底）。把下內距減少 20px，高度就少 20px，
上緣因此往下走 20px —— 裡面的內容跟著往下移，而且不會被裁切。

```css
.tabbar { padding-bottom: max(0px, calc(var(--safe-bottom) - 20px)); }
.entry-bottom { padding: 8px 8px max(0px, calc(8px + var(--safe-bottom) - 20px)); }
```

`max(0px, …)` 是給沒有 safe area 的裝置（Android、舊 iPhone）用的，
否則會算出負的內距。

**驗證**（402×812、safe-top 62、safe-bottom 34）

| 元素 | v1.22.1 | v1.23.0 | 位移 |
|---|---|---|---|
| 數字鍵盤上緣 | 396 | 416 | **+20** |
| 分頁列上緣 | 719 | 739 | **+20** |
| 分頁列圖示 | 730 | 750 | **+20** |
| 分頁列文字 | 751 | 771 | **+20** |

沒有任何東西被裁切：「完成」下緣 696、`.entry-bottom` 下緣 718（仍有 22px 內距）；
分頁列文字下緣 788、分頁列下緣 812（仍有 24px）。
291 項測試通過。

**備註**
前面幾輪我一直在推論「為什麼」版面會這樣，使用者要的是「照指示移動」。
這一版只做位移，不附帶任何推論。

---

## 2026-09-26 底部往下補足，取代硬位移（v1.23.2）

**需求**
使用者：「再往下 60px」（累計 80px）。

**為什麼不能照字面做**
v1.23.0 用「縮下內距」達成 20px，但那個做法有上限 ——
`.entry-bottom` 只有 42px、`.tabbar` 只有 34px 的內距可以吃，到頂就下不去了。

改用 `position: relative; top: 80px` 實測（v1.23.1）：
在 App 已經填滿視窗的環境下，**分頁列文字被推出畫面 36px**。
而且 `.entry-bottom` 在 `overflow-y: auto` 的捲動區裡，位移後「完成」也會被裁掉。
把文字切掉比底下空一塊更糟，不能這樣出。

**做法：長高，不是位移**

```css
.app { height: calc(100dvh + var(--bottom-shift)); }
```

`--bottom-shift` 由 `applyBottomShift()` 在執行時算出來：
先把它歸零，量 `innerHeight - app.bottom` 得到**底下真正空著多少**，
再補上那麼多（上限 80px）。

兩個刻意的設計：
- **只補到視窗底部為止**。補過頭就是把分頁列文字推出畫面
- **讓 App 長高而不是把面板往下推**。長高會讓上面的捲動區跟著變大，
  裡面的東西不會被裁掉；位移不會

在 `resize`、轉向、回到前景時重算。

**驗證**（402×812）

| 情境 | 量到的空隙 | 實際補足 | 分頁列文字 | 被裁切 |
|---|---|---|---|---|
| App 本來就填滿視窗 | 0 | 0 | 751–768 | 否 |
| 模擬 `100dvh` 短 62px | 62 | 62 | 751–768 | 否 |

模擬情境下 App 下緣由 750 回到 812，正好貼齊視窗底部。
診斷畫面新增「App 下方空隙」與「已往下補足」兩項。291 項測試通過。

**說明**
使用者要的是「往下 80px」，實際給的是「把底下空著的地方補滿，最多 80px」。
若裝置上根本沒有空隙，就不會有任何位移 —— 那代表空白在網頁外面，
不是這支程式動得到的，而診斷畫面的兩個數字會直接講出是哪一種。

---

## 2026-09-26 拆解「完成」與分頁列之間的 gap（v1.23.3）

**需求**
使用者指出兩件事，並要求給出物件名稱：
1. 記帳／明細那條 panel 沒有往下移動
2. 數字鍵盤那塊 panel 與它之間有 gap

**物件名稱**

| 使用者說的 | class | 位置 |
|---|---|---|
| 記帳／明細／資產／報表／設定 | `.tabbar`（`.tabbar__item` / `__icon` / `__label`） | `js/app.js` `buildTabBar()` |
| 數字鍵盤整塊（含「完成」） | `.entry-bottom` | `js/views/entry.js:101` |
| 純鍵盤 | `.keypad` | `js/views/entry.js:84` |
| 完成按鈕 | `.btn--submit` | `js/views/entry.js:85` |

**gap 的組成**（402×812、safe-bottom 34）

```
.btn--submit 底部
   ↓ 42px   .entry-bottom 的 padding-bottom = 8 + safe-bottom 34
   ↓ 21px   .install-banner（hidden 但仍佔位）
.tabbar 上緣
```

**做法**
移除其中的 21px：把 `[hidden] { display: none !important; }` 加回來。

這一條的歷程要記下來，免得再繞：
v1.18.2 修掉 → v1.22.1 依使用者要求還原（要回到 9:21 之前的位置，它是唯一差異）
→ 使用者指出那就是中間的 gap，v1.23.3 再次修掉。
反覆的原因是「還原到舊位置」與「消除 gap」這兩個要求本身互相衝突，
而我在 v1.22.1 沒有把這個衝突講出來就照做了。

**驗證**（402×812）

| 量測 | v1.23.2 | v1.23.3 |
|---|---|---|
| `.install-banner` 高 | 21px | **0px** |
| 「完成」到 `.tabbar` 的距離 | 63px | **42px** |
| `.entry-bottom` | 388–719（含 21px 幽靈帶在下方） | 388–719 |
| `.tabbar` | 719–812 | 719–812 |

剩下的 42px 就是 `.entry-bottom` 自己的下內距，是個獨立的數字，可以另外調。
291 項測試通過。

**待辦**
- 第 1 點（分頁列沒往下移）要看使用者裝置上「App 下方空隙／已往下補足」兩個數字，
  才知道是補不到還是根本沒空隙

---

## 2026-09-26 完整還原到 v1.18.0 版面供對照（v1.24.0）

**需求**
使用者：「還是無法改回原來的版面，`.tabbar` 和 iPhone 最底部有空隙。
還原到 9:21 AM 前的版本，讓我確認一下版面。」

**做法**
不再手改 CSS —— 直接從 git 取回 v1.18.0 的檔案：

```
git checkout 7955c0e -- css/app.css
```

`git diff 7955c0e -- css/app.css` 無輸出，**與 v1.18.0 逐字元相同**。

同時移除與該版本不相容的東西：
- `applyBottomShift()` 與 `--bottom-shift`（v1.18.0 的 CSS 沒有這個變數，留著是死碼）
- 啟動時強制把 safe-area 覆寫開關歸零 —— 開關若還開著，
  使用者看到的就不是 v1.18.0 的版面，這次的對照就沒有意義

**刻意保留（都不影響版面）**
- Service Worker 的更新機制（v1.20.x～v1.21.3）。
  若連這個一起退回，使用者會再次被鎖在舊版無法更新 ——
  那個坑已經踩過一次，不能為了還原版面再製造一次
- 版面診斷畫面

**驗證**（402×812、safe-top 62、safe-bottom 34）

| 元素 | 座標 | 說明 |
|---|---|---|
| `.app` | 0–812 | 填滿視窗 |
| `.btn--submit` | 610–656 | |
| `.install-banner` | 698–719（21px） | v1.18.0 的幽靈帶，如實還原 |
| `.tabbar` | 719–812 | |
| safe-area 覆寫 | 兩個都是 false | 已歸零 |

291 項測試通過。

**下一步的判斷依據**
若使用者在這一版**仍然**看到 `.tabbar` 與螢幕底部之間有空隙，
就證明那個空隙**不是今天任何一次改動造成的** ——
它是 iOS 給這個 App 的視窗（812）比螢幕（874）矮 62 點，
在網頁外面，CSS 動不到。那時應該停止在版面上繼續嘗試。

---

## 2026-09-26 再退一版到 v1.17.0；並查出底部版面從未改過（v1.25.0）

**需求**
使用者：v1.24.0 仍不是原來的版面，請再退前一版確認。

**做法**
退到 **v1.17.0**（09-07），那是 `.install-banner` 還不存在的最後一版：

```
git checkout 5574cdd -- css/app.css      # 與 v1.17.0 逐字元相同
```

並且**不把提示列掛上 DOM**（`SHOW_INSTALL_BANNER = false`）——
提示列是 v1.18.0 才加的，掛上去就不是 v1.17.0 的版面。
設定頁裡的「加到主畫面」說明與安裝按鈕不受影響。

**同時做了一件早就該做的事：查底部版面到底改過沒有**

把 `css/app.css` 每一個 commit 的 `.app` / `.tabbar` / `.entry-bottom` /
`.tabbar__item` 四條規則抓出來逐一比對，一路回到專案的第一個 commit（09-01）：

| 規則 | 09-01 首版 vs 現在 |
|---|---|
| `.app` | **相同** |
| `.tabbar` | **相同** |
| `.entry-bottom` | **相同** |
| `.tabbar__item` | **相同** |

```css
.app          { height: 100vh; height: 100dvh; }
.tabbar       { padding-bottom: var(--safe-bottom); flex-shrink: 0; }
.entry-bottom { padding: 8px 8px calc(8px + var(--safe-bottom)); }
```

**這四條規則從 09-01 專案開始到現在，一個字都沒變過。**
（唯一的例外是今天 11:23 的 v1.23.0，二十分鐘後就改回來了。）

**結論**
`.tabbar` 與 iPhone 螢幕底部之間的空隙，**不可能是任何一次改動造成的** ——
決定它位置的規則從來沒有被動過。再往前退任何版本都不會改變這個結果。

真正的原因先前已經量到：iOS 給這個主畫面捷徑的網頁視窗高 **812**，
螢幕卻是 **874**，差的 62 點在網頁外面。

**驗證**（402×812）
提示列不在 DOM；`.app` 0–812、「完成」631–677、`.entry-bottom` 388–719、
`.tabbar` 719–812、「完成」到分頁列 42px。291 項測試通過。

**待辦**
- 停止在版面上繼續嘗試。四種寫法（縮內距／position fixed／相對位移／撐高 App）
  都被同一件事擋住：App 拿不到視窗外的那塊空間。
- 若使用者仍要處理那 62 點，剩下的路不在這支程式裡（iOS 版本、
  改用 Safari 直接開而不加到主畫面）。

---

## 2026-09-26 改掉 iOS 的 status-bar-style（v1.26.0）

**關鍵回報**
使用者用 Safari 開：**沒有間隙**（底下是 Safari 自己的工具列）。
以主畫面 App 開：**有一大塊間隙**。

這把範圍縮到「iOS 怎麼配獨立 App 的網頁視窗」，而那由
`apple-mobile-web-app-status-bar-style` 決定 —— 不是 CSS。

**成因**
原本設的是 `black-translucent`，意思是「讓網頁畫到狀態列底下」。
實測在這台 iPhone 上，iOS 把視窗**貼在螢幕最上面**、卻只給
**812 點高**（螢幕 874），於是底部空出 62 點。
先前量到的所有數字（`innerHeight` 812、`safe-area 上` 62、
內容從螢幕 y≈74 開始）全部吻合這個描述。

**做法**
改成 `default`。iOS 會自己把視窗排在狀態列底下，視窗就一路延伸到螢幕底部。

代價：內容不再畫到狀態列底下，狀態列變成系統自己的底色。
換掉的是「底部空一塊」—— 划算。

**兩個必須先講明的但書**
1. **可能要重新加到主畫面**。iOS 在建立捷徑時會把這些 meta 記下來，
   不保證每次啟動都重讀。
2. **上方可能反過來多一塊**。若 iOS 把視窗排到狀態列底下之後，
   `env(safe-area-inset-top)` 仍然回報 62，那我們的 `padding-top`
   就變成多墊一次。診斷畫面的「試：取消上方內距」就是為此準備的。
   這個情況無法從 JS 可靠地事先判斷（同樣的數字在兩種配置下意義相反），
   所以留給使用者一鍵切換，不自動猜。

**驗證**
meta 已是 `default`，App 正常啟動，無主控台錯誤，291 項測試通過。
真正的效果只有在 iPhone 的獨立 App 模式下才看得出來。

**待辦**
- 使用者回報：底部間隙是否消失、上方是否多出一塊
- 若上方多出一塊 → 用「試：取消上方內距」，有效的話再考慮自動化

---

## 2026-09-26 上緣不墊 safe-area，下緣保留（v1.26.1）

**需求**
使用者測試 v1.26.0 後回報：
「上面加入會擋住，下面的加入有幫助，所以維持下面、上面不要加。」

**理由**
v1.26.0 把 `apple-mobile-web-app-status-bar-style` 改成 `default` 之後，
iOS 已經把網頁視窗排在狀態列底下 —— 狀態列那塊**不在視窗範圍內**。
這時再用 `env(safe-area-inset-top)` 墊一次，就是把內容白白往下推一截，
實測會擋住上方的內容。這正是 v1.26.0 就先寫明的「但書 2」。

下緣不同：home indicator 確實在視窗裡面，要閃開，所以 `--safe-bottom` 保留。

**做法**
```css
--safe-top: 0px;                                 /* 不再讀 env() */
--safe-bottom: env(safe-area-inset-bottom, 0px); /* 維持 */
```

**驗證**（402×812、注入 safe-bottom 34）

| 量測 | 值 |
|---|---|
| `--safe-top` | `0px` |
| `--safe-bottom` | `34px` |
| `.app__main` 上內距 | `0px` |
| `.tabbar` 下內距 | `34px` |
| `.app` | 0–812（填滿視窗） |

291 項測試通過。

**待辦**
- 使用者說「先改這一段」，後續還有其他要調整的部分

---

## 2026-09-26 版面改為跟著字體大小縮放（v1.27.0）

**需求**
使用者：「記帳和設定的頁面，上方的空間不夠。這可能和我使用的字體有關係，
我有增加字體大小，所以版面上會有點改變……需要考量字體大小也要放進去版面才不會跑掉。」

**這是今天最有用的一條線索**
使用者把系統字體調大了。而這份 CSS 從頭到尾用 `px` 寫死間距與高度 ——
字一放大，固定高度的容器就擠壓或裁切內容，固定 px 的留白則顯得太窄。
先前追了一整天的「版面跑掉」，很可能有一部分就是這個。

**改動**

| 位置 | 原本 | 改成 | 理由 |
|---|---|---|---|
| `.app__main` 上內距 | `var(--safe-top)`（＝0） | `calc(var(--safe-top) + 0.7em)` | 安全區為 0 後內容貼著視窗最上緣；用 `em` 才會跟著字體變大 |
| `.entry-top` 內距 | `10px 14px 6px` | `0.8em 14px 0.5em` | 字大時金額與分段列不要黏在一起 |
| `.tabbar__item` | `height: 58px` | `min-height: 58px` ＋ `padding: 6px 2px` | **固定高度會把放大的文字裁掉**；改成最小高度，字大就讓分頁列長高 |
| `.row__icon` | `30px` | `2em`（預設 15px 下正好 30px） | 寫死 px 會裁掉放大的 emoji |
| `.tx-row__icon` | `36px` | `2.4em` | 同上 |
| `.icon-btn` | `width/height: 38px` | `min-width/min-height: 38px` | 同上 |

**驗證**（402×812）

| 量測 | 預設字體 15px | 放大到 20px |
|---|---|---|
| `.app__main` 上內距 | 10.5px | **14px**（跟著變大） |
| 記帳頁分段列上緣 | 23（原本約 10） | — |
| 設定頁第一張卡上緣 | 23（原本約 12） | **26** |
| `.tabbar__item` | `min-height: 58px`，無固定 height | — |

291 項測試通過。

**待辦**
- 使用者確認上方空間是否足夠；不夠的話 `0.7em` 這個數字可以再加
- 其餘仍以 px 寫死的尺寸（表格列高、圖表等）視回報再逐步改成 `em`

---

## 2026-09-26 上方 safe-area 墊回來（v1.27.1）

**需求**
使用者附實機截圖：**iOS 狀態列（12:46、4G、電量）直接蓋在
「支出／收入／轉帳」那排按鈕上面**，並指出 iPhone 上方應該要加入空間。

**這張截圖推翻了 v1.26.x 的前提**
v1.26.0 把 `apple-mobile-web-app-status-bar-style` 改成 `default`，
照規格 iOS 應該把網頁視窗排在狀態列底下。v1.26.1 據此把 `--safe-top` 歸零。

實機顯示**不是這樣**：網頁仍然畫到狀態列底下（iOS 沿用了捷徑建立當時
記下的設定，meta 改了不生效）。上方內距一歸零，最上面那排按鈕就被狀態列蓋住。

**規格怎麼寫不重要，實機長什麼樣才算數。** 這一條要記住。

**做法**
`--safe-top` 改回 `env(safe-area-inset-top, 0px)`。
v1.27.0 加的 `+ 0.7em` 保留，兩者相加才是實際的上緣留白。

**驗證**（402×812、注入 safe-top 62、safe-bottom 34）

| 量測 | 值 |
|---|---|
| `.app__main` 上內距 | 72.5px（＝62 + 0.7em 10.5） |
| 記帳頁分段列上緣 | 85（狀態列 62 以下，留 23px 餘裕） |
| 設定頁第一張卡上緣 | 85 |

291 項測試通過。

**我的誤讀**
使用者先前說「上面加入會擋住」，我讀成「加上內距會擋住內容」，
於是在 v1.26.1 把它歸零。對照這張截圖，那句話更可能是在描述
「上面（狀態列）會擋住」這個現象本身。語意有歧義時應該先確認再動手。

